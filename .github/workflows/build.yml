name: Build
on:
  workflow_dispatch:
  push:
    branches-ignore:
      - data
      - gh-pages
    tags:
      - 'v*'
  pull_request:
    branches-ignore:
      - data
      - gh-pages

jobs:
  build-android:
    name: Build for Android
    runs-on: ubuntu-latest

    steps:
      # Мы убрали отдельный шаг checkout, так как buildozer-action сделает его сам внутри себя
      - name: Build with Buildozer
        uses: ArtemSBulgakov/buildozer-action@v1
        id: buildozer
        with:
          workdir: test_app
          buildozer_version: stable

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: ${{ steps.buildozer.outputs.filename }}
